{
  "rules": {
    ".read": false,
    ".write": true,
    "runs": {
      "$runId": {
        ".read": true,
        ".validate": "newData.exists()"
      }
    },
    "$other": {
      ".validate": false
    }
  }
}
